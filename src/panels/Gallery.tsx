import { type GalleryGridProps } from "../types"

export const GalleryPanel = ({
  state,
  onRefresh
}: GalleryGridProps) => {
  return (
    <div className="w-48 bg-gray-900 border-l border-gray-800 flex flex-col h-screen sticky top-0">
      GalleryPanel
    </div>
  )
}
